Notes on background:
  - handling the SVG in JS by DOM is probably really wasteful -- should be string

Notes on puzzle data:
  - compression depends on the order of tiles -- try to sort it differently
  - compression depends on the order of parameters (of tiles) -- probaly could save by arraging shape_index and rotation first (repeated stuffs aligned)
